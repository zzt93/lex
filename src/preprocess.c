#include "all_stdlib.h"

/**
   @return : return a newly allocated string, remember to free it
 */
char * expand(char *regex) {
    int est_len = ;
    char *res = malloc(est_len);
    return res;
}

/**
   @return : return a newly allocated string, remember to free it
 */
char * addDot(char *regex) {
    int est_len = strlen(regex) + count;
    char *res = malloc(est_len);
}